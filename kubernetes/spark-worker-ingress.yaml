apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spark-worker-ingress
  annotations:
spec:
  rules:
  - host: spark-kubernetes
    http:
      paths:
        - pathType: Prefix
          path: /
          backend:
            service:
              name: spark-worker
              port:
                number: 8081
